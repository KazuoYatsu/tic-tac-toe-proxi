class Player {
  /**
   * Instatiates a Player
   * @param {String} character - The String to represent the player in the game
   */
  constructor(character) {
    this.character = character;
  }
}

module.exports = {
  Player,
};
